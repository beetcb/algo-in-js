import * as tests from "./tests";

/**
 * Valid Parentheses
 */

function isValid(str) {
  const rightParenthesesToLeft = {
    "(": ")",
    "[": "]",
    "{": "}"
  };
  for (const char of str) {
  }
}
